import 'package:flutter/material.dart';

class TextBuild extends StatelessWidget {
  const TextBuild({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}